import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.jonnyramen.canvasdrawing',
  appName: 'Canvas Drawing',
  webDir: 'www'
};

export default config;
